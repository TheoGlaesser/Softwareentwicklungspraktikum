#include "Log.h"
/*
void Log::print_nodes(const std::vector<QPointF>& nodes) {
  std::cout << "nodes : "; 
  for(auto node : nodes) {
    std::cout << "(" << node.x() << "," << node.y() << ") ";
  }
  std::cout << "\n";
}

void Log::print_nodeItems(const std::vector<nodeGraphicsItem*>& nodeItems) {
  std::cout << "nodeGraphics: ";
  for(auto nodeItem : nodeItems) {
    std::cout << "(" << nodeItem->rect().center().x() << "," <<  nodeItem->rect().center().y() << ") ";
  }
  std::cout << "\n";
}



void Log::print_lines(const std::vector<QGraphicsLineItem*>& lines) {
  std::cout << "Line Graphics: ";
  for(auto line : lines) {
    std::cout << "((" << line->line().p1().x() << "," << line->line().p1().y() << ");(" << line->line().p2().x() << "," <<  line->line().p2().y() << ")) ";
  }
  std::cout << "\n";
}



void Log::print_forces(const std::vector<force>& forces) {
  std::cout << "forces: ";
  for(auto force : forces) {
    std::cout << "(" << force.point.x() << "," << force.point.y() << ") ";
  }
  std::cout << "\n";
}


void Log::print_forceGraphicsItems(const std::vector<forceGraphicsItem>& forceGraphicsItems) {
  std::cout << "forceGraphicsItems: ";
  for(int i = 0; i < forceGraphicsItems.size(); i++)
  {
    std::cout << "(" << forceGraphicsItems[i].forceLineItem->line().p1().x() << "," << forceGraphicsItems[i].forceLineItem->line().p1().y()  << ") ";
  }

  std::cout << "\n";
}


void Log::print_supportItems(const std::vector<QPointF>& points) {
  std::cout << "supportItems: ";
  for(auto point : points) {
    std::cout << "(" << point.x() << "," <<  point.y() << ") ";
  }
  std::cout << "\n";
}*/


